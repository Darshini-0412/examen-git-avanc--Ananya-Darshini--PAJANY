- Ma branche main est ma branche de base où j'ai fait quelques modifications sur le "README".
- Ma branche dev, est la nouvelle branche que j'ai crée où j'ai intégré le nouveau fichier "branches.md" dans mon dossier docs
- Ma branche feature sera une branche crée à partir de la branche dev pour les fonctionnalités nommées "feature-url"
- Pour finir ma branche hotfix, où je fais les corrections necessaires.

Exercice 2-5:
Pour mettre à jour la correction de l'erreur précédente dans le README, dans la branche de correction "hotfix/readme-correction" je fais ma correction puis je la merge avec les branches que je veux. Donc ici, j'ai merge avec la branche principale donc qui s'est corrigée et qui s'est aussi corrigé sur la branche dev puisque ma branche dev a été crée par main. Et donc lors de la mis à jour, l'erreur est rectifiée.